@model IEnumerable<Employee>
@{
    Layout="~/Views/SHared/_Layout.cshtml";
    ViewBag.Title = "Employee List"; 

}
@*<div class="card-deck">
    @foreach(var employee in Model)
    {
        <div class="card m-3">
            <div class="card-header">
                <h3>@employee.Name</h3>
            </div>
            <img class="card-img-top img-thumbnail" src=" ~../../Images/image_contact.png" />
            <div class="card-footer text-center">
                <a href="#" class="btn btn-primary">View</a>
                <a href="#" class="btn btn-primary">Edit</a>
                <a href="#" class="btn btn-danger">Delete</a>
            </div>
            </div>
    };
</div>*@
<br />
<h1>List of all Employees</h1>
<br />

<div class="card-deck row row-cols-1 row-cols-md-3">
@foreach (var employee in Model)
    {
        var Photopath = "~/../../images/" + employee.PhotoPath ?? "~/../../images/image_contact.png"

        ;

        <div class="card mx-auto justify-content-center align-content-center" style="width: 18rem;">
    
            <img src=@Photopath class="card-img-top imageThumbnail"  >
            <div class="col mb-4">
            <div class="card-body">
                <h5 class="card-title">@employee.Name</h5>
                 @*<p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                *@
                 <a asp-controller="Home" asp-action="details" asp-route-id="@employee.Id" class="btn btn-primary m-1">View</a>
                    <a asp-controller="Home" asp-action="edit" asp-route-id="@employee.Id" class="btn btn-success m-1">Edit</a>
                <a href="#" class="btn btn-danger m-1">Delete</a>
            </div>
            </div>
            <br />

    
    </div>
}
</div> 